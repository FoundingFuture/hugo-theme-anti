# Project memory

Durable knowledge about this repository, one file per topic. Record what the code and `git log` cannot say: decisions, the reasons behind them and constraints from outside the repo.

Name each file after its topic, in kebab-case, with a `.md` extension.

## Entries

- [Theme decisions](theme-decisions.md): name, Hugo floor, font, menu from content directories, site-only configuration, and hand checks.
